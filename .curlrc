# Show status
-w "[%{http_code}] %{time_total}s %{url_effective} (%{size_download} B) [%{exitcode}] \n"

# follow redirects
--location

--silent
--show-error
--fail
--compressed
--http2
--keepalive-time 120
